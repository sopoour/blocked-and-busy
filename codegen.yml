overwrite: true
schema: ${CONTENTFUL_GRAPHQL_ENDPOINT}
documents: 'src/services/graphql/**/!(types).ts'
generates:
  ./app/services/graphql/types.ts:
    plugins:
      - typescript
      - typescript-operations
